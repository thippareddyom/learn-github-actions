name: CI1
on:
    push:
        branches: ["!main"]
    workflow_dispatch:
jobs: 
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: run one shell script
              run: echo "Hello world"
            - name: run a multi line script
              run: |
                echo add other project
                echo test